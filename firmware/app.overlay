/*
 * Mark gpio0 as a wakeup-source device so the Zephyr ESP32 GPIO driver
 * will call esp_sleep_enable_ext1_wakeup_io() for any pin configured with
 * GPIO_INT_TRIG_WAKE_LOW, enabling GPIO wakeup from deep sleep.
 */
&gpio0 {
	wakeup-source;
};
